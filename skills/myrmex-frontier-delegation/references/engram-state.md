# Compatibility note

The authoritative persistence contract is `local-and-engram-state.md`.

Exact operational state belongs to the atomic `myrmex-state` store. Engram is the semantic continuity layer and is written only by the primary orchestrator.
