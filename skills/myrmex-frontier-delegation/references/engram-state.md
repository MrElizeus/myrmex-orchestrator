# Compatibility note

The authoritative persistence contract is `local-and-engram-state.md`.

Exact operational state belongs to atomic `myrmex-state`. Native
`myrmex-memory` owns evidence-backed project-memory records, while Engram is an
optional semantic adapter written only by the primary orchestrator. Neither
semantic backend may substitute for exact run receipts or transitions.
