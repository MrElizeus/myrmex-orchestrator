---
description: Force the agile direct route for a clear bounded task
agent: myrmex-orchestrator
---

Execute this objective using the Myrmex **DIRECT** route:

```text
$ARGUMENTS
```

Inspect Git state, protect pre-existing work, make the smallest repository-consistent change, run proportionate focused verification, inspect the final diff, and report evidence. Do not invoke SDD, frontier, or subagents unless the requested action becomes unsafe or materially impossible without escalation; in that case stop and report the blocker rather than silently changing routes.
